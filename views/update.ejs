<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            box-sizing: border-box;
        }

        .flex {
            display: flex;
        }

        body {
            width: 100%;
            height: 100vh;
            align-items: center;
            justify-content: center;
            gap: 20px;
        }

        .card {
            padding-inline: 20px;
            width: 500px;
            background: #fff;
            box-shadow: 0 0 12px rgba(0, 0, 0, 0.5);
            border-radius: 20px;
        }

        .form_inputs {
            flex-direction: column;
            align-items: center;
            padding: 20px;
            gap: 10px;
        }

        .form_inputs input {
            width: 100%;
            padding: 7px;
            border-radius: 10px;
            outline: none;
            border: 0.5px solid rgba(0, 0, 0, 0.438);
        }

        .form_inputs textarea {
            width: 100%;

        }

        .service_details {
            padding: 20px;
            flex-direction:  column;
            gap: 20px;
            justify-content: center;
            align-items: center;
        }
        .detail{
            gap: 20px;
        }

        .service_details img {
            height: 50px;
            aspect-ratio: 1;
        }
        .btn{
            padding: 10px;
            background: rgba(238, 43, 43, 0.74);
            color: #fff;
            border: none;
            outline: none;
            border-radius: 10px ;
        }
    </style>
</head>
<body class="flex">
    <div class="card">
        <form action="/api/service/update/<%= service._id  %>" method="post" enctype="multipart/form-data">
            <div class="form_inputs flex">
                <input type="text" name="name" value="<%= service.name  %>">
                <textarea name="description" id="description" cols="30" rows="10"><%= service.description  %></textarea>
                <input type="hidden" name="oldimage" value="<%= service.image %>">
                <input type="file" name="image" >
                <button class="btn">Update</button>
            </div>
        </form>
    </div>
</body>
</html>